<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Football Teams</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800">
    <div class="container mx-auto py-10 px-4">
        <h1 class="text-4xl font-bold text-center text-green-700 mb-10">Football Teams</h1>

        {% if teams|length > 0 %}
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
                {% for team in teams %}
                    <a href="/teams/{{ team.id }}" class="block bg-white p-6 rounded-2xl shadow hover:shadow-lg transition">
                        {% if team.logo %}
    <img src="{{ team.logo.getFullPath() }}" alt="{{ team.teamName }}" class="w-full h-40 object-contain mb-4 rounded" />
{% else %}
    <div class="w-full h-40 flex items-center justify-center bg-gray-200 text-gray-500 rounded">
        No Logo
    </div>
{% endif %}

                        <h2 class="text-2xl font-semibold text-green-600 mb-2">{{ team.teamName }}</h2>
                        <p><span class="font-semibold">Members:</span> {{ team.players|length }}</p>
                    </a>
                {% endfor %}
            </div>
        {% else %}
            <p class="text-center text-gray-500 italic">No teams found.</p>
        {% endif %}
    </div>
</body>
</html>
