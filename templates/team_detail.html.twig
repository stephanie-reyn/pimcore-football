<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ team.teamName }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800">
    <div class="max-w-4xl mx-auto px-6 py-10">
        <a href="/teams" class="text-sm text-blue-600 hover:underline">&larr; Back to list</a>

        <div class="mt-4 bg-white rounded-xl shadow p-6">
            {% if team.logo %}
                <img src="{{ team.logo.getFullPath() }}" alt="{{ team.teamName }}" class="w-full h-40 object-contain mb-4 rounded" />
            {% else %}
                <div class="w-full h-40 flex items-center justify-center bg-gray-200 text-gray-500 rounded">
                    No Logo
                </div>
            {% endif %}

            <h1 class="text-3xl font-bold text-green-700">{{ team.teamName }}</h1>
            <p class="mt-2 text-gray-700"><strong>Coach:</strong> {{ team.couch }}</p>
            <p class="text-gray-700"><strong>City:</strong> {{ team.city }}</p>
            <p class="text-gray-700"><strong>Founded:</strong> {{ team.foundedIn }}</p>

            {% if team.location %}
                <p class="text-gray-700"><strong>Location:</strong> Latitude {{ team.location.latitude }}, Longitude {{ team.location.longitude }}</p>
            {% endif %}

            <h2 class="mt-8 text-xl font-semibold text-gray-800">Players ({{ team.players|length }})</h2>

            {% if team.players|length > 0 %}
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-4">
                    {% for player in team.players %}
                        <div class="bg-gray-50 border rounded-lg p-4 shadow-sm hover:shadow transition">
                            <h3 class="text-lg font-bold text-green-700">{{ player.name }}</h3>
                            <p class="text-sm text-gray-600">#{{ player.number }} – {{ player.position }}</p>
                            <p class="text-sm text-gray-600">Age: {{ player.age }}</p>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <p class="text-sm text-gray-500 italic mt-2">No players assigned yet.</p>
            {% endif %}
        </div>
    </div>
</body>
</html>
